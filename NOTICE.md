# Safety Notice

This project is read-only by design. It scans files and prints recovery guidance.

It does not remove malware, revoke credentials, execute package lifecycle scripts,
or prove that a host is clean.
