{
  "name": "supply-chain-check",
  "version": "0.1.0",
  "description": "Read-only dependency and source risk checker for pre-execution supply-chain review.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "supply-chain-check": "./bin/supply-chain-check.js"
  },
  "scripts": {
    "scan": "node bin/supply-chain-check.js",
    "smoke": "node bin/supply-chain-check.js --help",
    "test": "node --check bin/supply-chain-check.js && node --check src/scanner.js && node test/smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
